[a b c d e f] = changeDevice(7);
avg = [1:20]*0;
for i=1:8
x = b{i};
for j=1:length(x)
avg(j) = avg(j) + x(j);
end
end
avg = avg/8
avg_1 = [1:20]*0;
for i=1:8
x = c{i}
for j=1:length(x)
avg_1(j) = avg_1(j) + x(j);
end
end
avg_1 = avg_1 /8;